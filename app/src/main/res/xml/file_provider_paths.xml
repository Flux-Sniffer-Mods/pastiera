<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <cache-path
        name="debug_reports"
        path="debug-reports/" />
    <!-- GIFs sent to apps (KlipyGifs.CACHE_FOLDER) -->
    <cache-path
        name="gifs"
        path="gifs/" />
    <!-- Flux Keyboard updates handed to the package installer (ForkUpdateInstaller) -->
    <cache-path
        name="updates"
        path="updates/" />
</paths>
